#pragma once

#include "common.h"
#include <stdint.h>
#include <vesc_types.h>

#define VESC_TELEM_CFG_POWER (1U << 0)
#define VESC_TELEM_CFG_RPM (1U << 1)
#define VESC_TELEM_CFG_TEMPERATURE (1U << 2)
#define VESC_CFG_TCP_BRIDGE (1U << 3)
